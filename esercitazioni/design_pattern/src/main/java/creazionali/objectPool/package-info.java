package creazionali.objectPool;


/**
 * ObjectPool
 *
 * Quando gli oggetti sono costosi da creare e sono
 * necessari solo per un piccolo periodo o poche azioni, ci si può
 * avvalere del Object Pool Pattern, che provvede a creare una cache per
 * gli oggetti istanziati e tracciarli (chi è in uso e chi è disponibile).
 */